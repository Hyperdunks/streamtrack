{
    "name": "streamtrack-backend",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "bun run --env-file ../.env scripts/check-env.ts && bun run --env-file ../.env --watch src/index.ts",
        "build": "bun build src/index.ts --outdir dist --target bun",
        "start": "bun run --env-file ../.env dist/index.js",
        "typecheck": "tsc --noEmit",
        "lint": "eslint src --ext .ts",
        "test": "bun test",
        "check-env": "bun run --env-file ../.env scripts/check-env.ts",
        "generate:intro-doc": "bun run scripts/generate-intro-doc.ts",
        "generate:project-doc": "bun run scripts/generate-project-doc.ts"
    },
    "dependencies": {
        "cors": "^2.8.6",
        "docx": "^9.6.1",
        "express": "^4.22.1",
        "firebase-admin": "^13.7.0",
        "mongoose": "^8.23.0"
    },
    "devDependencies": {
        "@types/bun": "^1.3.11",
        "@types/cors": "^2.8.19",
        "@types/express": "^5.0.6",
        "@types/node": "^22.19.15",
        "@types/supertest": "^6.0.3",
        "supertest": "^7.2.2",
        "typescript": "^5.9.3"
    }
}
